<%= render 'users/user_info', user: @user %>
<h2>Books</h2>
<table>
<tr>
<th></th>
<th>Title</th>
 <th>Opnion</th></tr>
<% @users.each do |users| %>
<% @books.each do |book| %>
<tr>
  <td><%= link_to image_tag(users.get_image(100,100)), user_path(users.id) %></td>
  <td><%= link_to book.title,  book_path(users.id) %></td>
  <td><%= book.body %></td>
</tr>
 <% end %>
 <% end %>
 </table>